<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <RootNamespace>M365Migrate.Core</RootNamespace>
    <AssemblyName>M365Migrate.Core</AssemblyName>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Azure.Identity" Version="1.13.1" />
  </ItemGroup>

  <ItemGroup>
    <!-- Expose internal helpers (e.g. UsersWorkload.DecodeExtUpn) to the test assembly. -->
    <InternalsVisibleTo Include="M365Migrate.Core.Tests" />
  </ItemGroup>

</Project>
